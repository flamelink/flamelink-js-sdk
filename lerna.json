{
  "version": "1.0.0-rc.1",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "allowBranch": "master",
      "ignoreChanges": [
        "fixtures/**",
        "integration/**",
        "*.md",
        "**/__tests__/**"
      ],
      "message": "chore(release): Publish %s to NPM"
    },
    "version": {
      "allowBranch": "master",
      "ignoreChanges": [
        "fixtures/**",
        "integration/**",
        "*.md",
        "**/__tests__/**"
      ]
    }
  }
}
